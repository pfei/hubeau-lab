[project]
name = "hubeau-lab"
version = "0.1.0"
description = "Real-world usage notebooks for hubeau-data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hubeau-data>=0.3.0",
    "jupyter",
    "pandas",
    "matplotlib",
    "plotly>=6.8.0",
]

[tool.uv]
package = false
